import { Separator } from "@base-ui/react/separator";
import type { ReactNode } from "react";

export const DesignerToolbarSeparator = () => {
	return <Separator orientation="vertical" className="h-4 w-px bg-border" />;
};
